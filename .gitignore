/target
**/*.rs.bk
.env
.DS_Store

# runtime artefacts
*.redb
abi.json
nuthatch.toml
# …but the committed example roost's nest configs are intentional (no secrets)
!examples/**/nuthatch.toml

# guest component build dirs (the staged component .wasm at components/*.wasm IS committed)
components/*/target

# local smoke-test nest (scripts/try-nuthatch.sh)
nuthatch-trial/
